CREATE TABLE companys_info
	(
		id int primary key auto_increment
		, company varchar(50)
		, url varchar(2083)
		, employees varchar(8)
		, capital varchar(15)
		, page int
		, source varchar(20) 
		, add_date datetime
	)
;